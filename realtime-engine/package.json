{
  "name": "poc-ams-realtime-engine",
  "version": "1.0.0",
  "description": "POC-AMS Real-Time Engine — Node.js 20 + Socket.io + LiveKit SDK (FRD Section 4.4)",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "socket.io": "^4.7.5",
    "livekit-server-sdk": "^2.6.1",
    "ioredis": "^5.4.1",
    "jsonwebtoken": "^9.0.2",
    "dotenv": "^16.4.5",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
